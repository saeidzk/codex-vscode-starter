# Durable Project Memory

This file contains verified facts and decisions that future contributors and
agents are likely to need. It is not a chat transcript, task log, or substitute
for Git history. Never store secrets, credentials, personal data, or speculation.

## Stable facts

- `<FACT>` — evidence: `<FILE_ISSUE_OR_SOURCE>` — verified: `<YYYY-MM-DD>`

## Decisions

### `<YYYY-MM-DD> — <DECISION_TITLE>`

- Decision: `<WHAT_WAS_DECIDED>`
- Reason: `<WHY>`
- Alternatives considered: `<ALTERNATIVES>`
- Consequences: `<TRADE_OFFS_OR_FOLLOW_UP>`
- Evidence: `<ADR_ISSUE_COMMIT_OR_DOCUMENT>`

## Domain conventions

- `<TERM>` means `<PRECISE_PROJECT_MEANING>`.
- `<UNIT_COORDINATE_OR_SIGN_CONVENTION>`.

## Validated reference results

| Case | Expected result | Tolerance | Evidence/date |
|---|---:|---:|---|
| `<BENCHMARK>` | `<VALUE_AND_UNIT>` | `<TOLERANCE>` | `<SOURCE>` |

## Known constraints

- `<CONSTRAINT>` — reason: `<WHY>` — revisit when: `<CONDITION>`

## Failed approaches worth remembering

- `<APPROACH>` failed because `<VERIFIED_REASON>`; evidence: `<REFERENCE>`.

## Maintenance

- Add only durable, verified information.
- Correct stale facts instead of appending contradictory entries.
- Move lengthy decisions to an ADR and link them here.
- Remove completed temporary work; track active tasks in the issue tracker or
  `PLANS.md` instead.

